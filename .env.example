# Base de datos PostgreSQL (Railway provee esta variable automáticamente si usas el plugin)
DATABASE_URL="postgresql://postgres:postgres123@localhost:5432/remision_db"

# JWT
JWT_SECRET="cambia_esto_por_un_secreto_de_al_menos_64_caracteres_aleatorios"
JWT_EXPIRATION="8h"

# CORS - URL del frontend en Railway (separar con coma si son varios)
CORS_ORIGIN="https://tu-frontend.up.railway.app"

# Puerto (Railway lo inyecta automáticamente, no cambiar)
# PORT=3000
